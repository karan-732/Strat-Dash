# The Python backend — agents, the sprint brain and Turso live there.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# The generation model, shown behind the i in the header.
NEXT_PUBLIC_GENERATION_MODEL=anthropic/claude-sonnet-5
